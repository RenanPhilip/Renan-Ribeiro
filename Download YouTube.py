from pytube import *

yt = YouTube(input('Cole aqui sua url: *))

print(yt.title) & Para descobrir o título do vídeo
print(yt.thumbnail url) & Para receber a url da thumb
stream = yt.streams.get highest resolution() %* Melhor reso!
stream.download()
